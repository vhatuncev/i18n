name: Sandbox

env:
  config: Release
  solutionName: src\i18n.sln
  
on:  
  push:
    branches:
      - "nuget_publish"

jobs:
  build:
    runs-on: [ windows-latest ]
    steps:
      - name: run script
        shell: pwsh
        run: | 
          updateVersion.ps1
        